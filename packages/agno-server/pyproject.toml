[project]
name = "agno-server"
version = "0.1.0"
description = "Python agno service exposing an AG-UI endpoint for the agui-server proxy."
requires-python = ">=3.10"
dependencies = [
  "agno>=2.7.0",
  "ag-ui-protocol>=0.1.9",
  "fastapi>=0.115.0",
  "openai>=1.50.0",
  "uvicorn[standard]>=0.30.0",
]

[tool.uv]
package = false
